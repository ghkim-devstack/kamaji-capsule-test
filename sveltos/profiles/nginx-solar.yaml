apiVersion: config.projectsveltos.io/v1beta1
kind: ClusterProfile
metadata:
  name: nginx-solar
spec:
  clusterSelector:
    matchLabels:
      env: tenant-b
  syncMode: Continuous
  policyRefs:
    - kind: ConfigMap
      namespace: projectsveltos
      name: nginx-solar-manifests
