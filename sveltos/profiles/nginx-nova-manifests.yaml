apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-nova-manifests
  namespace: projectsveltos
data:
  namespace.yaml: |
    apiVersion: v1
    kind: Namespace
    metadata:
      name: nova-dev
      labels:
        capsule.clastix.io/tenant: nova

  deployment.yaml: |
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: nginx-nova
      namespace: nova-dev
    spec:
      replicas: 1
      selector:
        matchLabels:
          app: nginx-nova
      template:
        metadata:
          labels:
            app: nginx-nova
        spec:
          containers:
            - name: nginx
              image: nginx:1.25
              ports:
                - containerPort: 80

  service.yaml: |
    apiVersion: v1
    kind: Service
    metadata:
      name: nginx-nova
      namespace: nova-dev
    spec:
      type: ClusterIP
      selector:
        app: nginx-nova
      ports:
        - port: 80
          targetPort: 80
