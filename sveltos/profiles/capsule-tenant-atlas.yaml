apiVersion: config.projectsveltos.io/v1beta1
kind: ClusterProfile
metadata:
  name: capsule-tenant-atlas
spec:
  clusterSelector:
    matchLabels:
      env: tenant-b
  syncMode: Continuous
  policyRefs:
    - kind: ConfigMap
      name: capsule-tenant-atlas-manifests
      namespace: projectsveltos
